{
  "mcpServers": {
    "kordoc": {
      "command": "npx",
      "args": ["-y", "-p", "kordoc@4.10.0", "kordoc-mcp"],
      "description": "한국 공문서(HWP3-5/HWPX/PDF/XLSX/DOCX) 파싱·서식입력·직인·비식별화 — npm kordoc 패키지의 MCP 서버 바이너리(kordoc-mcp). 법률 문서 무결성을 위해 버전 고정(kordoc@4.10.0). 툴 스펙은 mcp/kordoc/*.json 참조"
    },
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp@4.0.4"],
      "description": "Context7 공식 라이브러리 문서 조회 (Upstash 공식 패키지, 버전 고정 4.0.4: resolve-library-id → query-docs)"
    },
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@0.0.79"],
      "description": "웹 자동화·채증 (microsoft playwright-mcp, 버전 고정 0.0.79) — 게시물·SNS·기사 스크린샷/PDF 캡처. 캡처 직후 scripts/certify_evidence_file.py로 URL·시각·해시를 결합해 증거 기록을 만든다"
    },
    "sequential-thinking": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sequential-thinking@2026.7.4"],
      "description": "순차적 심층 추론 (modelcontextprotocol 공식 서버, 버전 고정 2026.7.4) — 포렌식 인과관계 역추적·다층 법률 쟁점 분석에서 단계별 가설 검증에 사용. 실행은 로컬 노드로만 이뤄진다"
    },
    "korean_law": {
      "command": "node",
      "args": ["./scripts/korean_law_mcp_wrapper.mjs"],
      "description": "국가법령정보센터/대법원 판례 검색 — lazyforensic API 서버 또는 lazyantigravity 오프라인 그라운딩 서버 자동 연동 (optional)",
      "optional": true,
      "env": {}
    }
  }
}
