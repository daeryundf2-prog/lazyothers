# lazyothers Python dependencies
# 법증 도구 체인이므로 상한을 고정한다(다음 메이저까지) — 갑작스런 상위
# 버전 동작 변경으로 산출물이 달라지는 것을 막는다.
# Core (required for HWPX/HWP/PDF parsing)
olefile>=0.47,<1
pymupdf>=1.23,<2
pypdf>=4.0,<7

# Financial flow tracer — XLSX 거래내역 읽기 (CSV는 표준 라이브러리로 처리)
openpyxl>=3.1,<4

# Optional — enhanced HWP 5.0 parsing
hwp-hwpx-parser>=0.4,<1

# Optional — OCR for scanned PDFs
# docling>=2.0
# unstructured>=0.12

# Optional — Kiwi 형태소 그라운딩 (`--morph-grounding`). 없으면 정규식 폴백으로 정합성 감사 품질이 떨어진다.
kiwipiepy>=0.20,<1

# Firecrawl AnyDoc — Universal Office & Document to Markdown converter (DOCX, PPTX, XLSX, etc.)
firecrawl-anydoc>=0.2.4

# Dev
pytest>=7.0,<9
